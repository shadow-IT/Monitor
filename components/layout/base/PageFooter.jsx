import Typography from '@material-ui/core/Typography';

const PageFooter = () => (
    <Typography >
        Yo dawg, here's a footer.
    </Typography>
);

PageFooter.displayName = 'PageFooter';

export default PageFooter;
